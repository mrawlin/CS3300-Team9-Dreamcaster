<%= form_for @dream do |f| %>

  <% if @dream.errors.any? %>
    <div id="error_explanation">
	  <h2>
		<%= pluralize(@dream.errors.count, "error") %> prohibited this article from being saved:
	  </h2>
	  <ul>
		<% @article.errors.full_messages.each do |msg| %>
		  <li><%= msg %></li>
			<% end %>
	  </ul>
	</div>
<% end %>

	<p>
	  <%= f.label :"Describe your dream in a few single words" %><br><br>
	  <%= f.text_field :title, size: 50 %>
	</p>

	<p>
	  <%= f.label :"Tell us about your dream..." %><br><br>
	  <%= f.text_area :text, cols:49, rows:20%>
	</p>

	<p>
	  <%= f.submit %> <%= link_to '<button type="button">Cancel</button>'.html_safe, dreams_path %>
	</p>

<% end %>